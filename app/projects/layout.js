export default function ProjectsLayout({ children }) {
    return (
        <div>
            <header className="red">
                Header Name
            </header>
            {children}
        </div>
    );
}
